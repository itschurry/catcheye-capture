name: catcheye-capture-raspbian
services:
  catcheye-capture-develop-arm64:
    extends:
      file: docker-compose.base.yml
      service: base-service
    image: ghcr.io/itschurry/catcheye-capture:develop-arm64
    container_name: catcheye-capture-develop-arm64
    working_dir: /home/user/catcheye-capture
    privileged: true
    environment:
      - BUILD_TYPE=develop
    volumes:
      - ../../:/home/user/catcheye-capture:rw
      - ${HOME}/shared:/mnt/shared:rw
      - ${HOME}/.ssh:/home/user/.ssh:ro

    command: bash
    stdin_open: true
    tty: true
